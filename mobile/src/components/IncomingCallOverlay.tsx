import React from 'react';

// Incoming call detection will be rebuilt using Stream Video SDK's
// native push/event system. This component is intentionally empty.
export function IncomingCallOverlay() {
  return null;
}
